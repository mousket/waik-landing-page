# Task 08a — Shared server helper: effective admin `facilityId`

**Phase:** 3e  
**Status:** DONE (2026-04-25)

## Delivered

- `lib/effective-admin-facility.ts` — `resolveEffectiveAdminFacility` + `isEffectiveAdminFacilityError`.
- Replaces duplicated logic that lived in `GET /api/admin/dashboard-stats` and `GET /api/incidents`.

## When done

Set **Status: DONE** (add date) and **rename** this file to `task-08a-effective-admin-facility-done.md`.
