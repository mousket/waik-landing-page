# Task 08e — Admin list pages: real data

**Phase:** 3e  
**Status:** DONE (2026-04-25)

## Delivered

- `/admin/incidents` — client list from `GET /api/incidents` with facility context; **View** links preserve query.
- `/admin/assessments` — table from `GET /api/admin/assessments` with facility context.

## When done

Set **Status: DONE** (add date) and **rename** this file to `task-08e-admin-list-pages-real-data-done.md`.
