# Task 08d — Client navigation: preserve `facilityId` + `organizationId`

**Phase:** 3e  
**Status:** DONE (2026-04-25)

## Delivered

- `getAdminContextQueryString`, `buildAdminIncidentsApiPath` in `lib/admin-nav-context.ts` for API + list URLs.
- Admin settings index **Staff** link; residents + staff API calls; **Stats** sidebar (assessments + intelligence); incident detail **Back to dashboard**; dashboard **Needs attention**, **Active**, **closed** tab fetches; `active-investigations-tab` and `needs-attention-tab` incident links.

## When done

Set **Status: DONE** (add date) and **rename** this file to `task-08d-admin-nav-context-preservation-done.md`.
