# Task 08c — Admin write APIs: facility scope

**Phase:** 3e  
**Status:** DONE (2026-04-25)

## Delivered

- `POST /api/residents` — effective facility from request URL (and optional body hints in resolver).
- `POST` staff invite, import, import/confirm; `PATCH` deactivate, reactivate, role; `POST` resend-invite — all resolve facility from **URL query** (and invite/confirm still accept optional body hints in resolver).

## When done

Set **Status: DONE** (add date) and **rename** this file to `task-08c-admin-write-apis-facility-scope-done.md`.
